[project]
name = "stratum-backend"
version = "0.1.0"
description = "Stratum backend bootstrap"
readme = "../README.md"
requires-python = ">=3.11,<3.12"
dependencies = [
    "fastapi",
    "httpx",
    "pytest",
    "uvicorn",
]

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
